const name = "Adbeel Omiunu";
const Id = "HNG-02582";
const language = "JavaScript";
const email = "adbeelomiunu@gmail.com";

let sayHelloWorld = (name, id, lang, email) => {
console.log(`Hello world, this is ${name} with HNGi7 ID ${id} using ${lang} for stage 2 task.${email}`);
};

sayHelloWorld(name, Id, language, email);
