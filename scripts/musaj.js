const name = "Muhammad Salihu"
const id = "HNG-00541"
const language = "javascript"
const email = "muhammad.salihu1@yahoo.com"

const introduce = (name, id, language, email) => {
  console.log(
    `Hello world, this is ${name} with HNGi7 ID ${id} using ${language} for stage 2 task.${email}`
  )
}

introduce(name, id, language, email)
