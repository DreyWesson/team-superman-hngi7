function hngInfo(fullname, id, language) {
 const info = `Hello World, this is ${fullname} with HNGi7 ID ${id} using ${language} for stage 2 task.`;
 console.log(info);
};

const fullName = "Ogundare Olamide";
const hngID = "HNG-04678";
const language = "JavaScript";

hngInfo(fullName, hngID, language);
