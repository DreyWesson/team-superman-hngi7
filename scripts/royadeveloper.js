const name = "Prince Chimaobi";
const id = "HNG-00996";
const email = "royadeveloper01@gmail.com";
const language = "JavaScript";

const myInfo = (name, id, language) => {
    console.log(`Hello World, This is ${name} with HNGi7 ID ${id} using ${language} for stage two task.${email}`);
}
myInfo(name,id,language);
